import { useEffect, useMemo, useState } from 'react'
import { Copy, Plus, Save, ShieldCheck, Workflow, X } from 'lucide-react'
import toast from 'react-hot-toast'
import { type ERPAccessRole, type ERPFieldType, type ERPFormField, type ERPWorkflow, useAssignERPAccountRole, useAssignERPTeamRole, useCloneERPAccessRole, useCreateERPAccessRole, useDeactivateERPAccessRole, useERPCatalog, useERPAccessRoles, useERPForm, useManagedAccounts, usePublishERPForm, useSaveERPFormDraft, useTeams, useUpdateERPAccessRole } from '../api/enterprise'

const DEFAULT_WORKFLOW: ERPWorkflow = {
  initial_state: 'draft',
  states: [{ key: 'draft', label: 'Draft' }, { key: 'submitted', label: 'Submitted' }, { key: 'approved', label: 'Approved', terminal: true }, { key: 'rejected', label: 'Rejected', terminal: true }, { key: 'cancelled', label: 'Cancelled', terminal: true }],
  transitions: [{ from: 'draft', to: 'submitted', label: 'Submit', role_ids: [], requester_allowed: true }, { from: 'submitted', to: 'approved', label: 'Approve', role_ids: [], requester_allowed: false }, { from: 'submitted', to: 'rejected', label: 'Reject', role_ids: [], requester_allowed: false }, { from: 'draft', to: 'cancelled', label: 'Cancel', role_ids: [], requester_allowed: true }],
}

const emptyField = (section: ERPFormField['section']): ERPFormField => ({ key: '', label: '', field_type: 'text', section, required: false, options: {}, validation: {}, position: 0 })

function RoleEditor({ roles, operations, actions }: { roles: ERPAccessRole[]; operations: Record<string, { label: string }>; actions: string[] }) {
  const create = useCreateERPAccessRole(); const update = useUpdateERPAccessRole(); const clone = useCloneERPAccessRole(); const deactivate = useDeactivateERPAccessRole()
  const accounts = useManagedAccounts(); const teams = useTeams(); const assignAccount = useAssignERPAccountRole(); const assignTeam = useAssignERPTeamRole()
  const [selectedId, setSelectedId] = useState<number | null>(null); const selected = roles.find((role) => role.id === selectedId)
  const [name, setName] = useState(''); const [code, setCode] = useState(''); const [description, setDescription] = useState(''); const [capabilities, setCapabilities] = useState<Array<{ resource: string; action: string }>>([])
  useEffect(() => { if (!selected) { setName(''); setCode(''); setDescription(''); setCapabilities([]); return }; setName(selected.name); setCode(selected.code); setDescription(selected.description || ''); setCapabilities(selected.capabilities) }, [selectedId, selected])
  const toggle = (resource: string, action: string) => setCapabilities((current) => current.some((item) => item.resource === resource && item.action === action) ? current.filter((item) => item.resource !== resource || item.action !== action) : [...current, { resource, action }])
  const save = async () => { try { if (selected) await update.mutateAsync({ id: selected.id, name, description, capabilities }); else { if (!name || !code) return toast.error('Role name and code are required'); await create.mutateAsync({ name, code, description, capabilities }) }; toast.success('Role saved'); setSelectedId(null) } catch (error: any) { toast.error(error.response?.data?.detail?.code || error.response?.data?.detail || 'Role could not be saved') } }
  return <section className="panel erp-builder-panel"><div className="panel-heading"><div><span className="eyebrow">ACCESS ROLES</span><h3>Operation permissions</h3><p>Grant business operations and actions, then assign the role to people or teams.</p></div><ShieldCheck /></div><div className="erp-builder-split"><aside className="erp-builder-list"><button className={!selected ? 'active' : ''} onClick={() => setSelectedId(null)}><Plus size={14} /> New role</button>{roles.map((role) => <button key={role.id} className={role.id === selectedId ? 'active' : ''} onClick={() => setSelectedId(role.id)}><span>{role.name}</span><small>{role.is_system ? 'System' : role.is_active ? 'Active' : 'Inactive'}</small></button>)}</aside><div className="erp-builder-editor"><div className="erp-builder-row"><label>Name<input value={name} onChange={(event) => setName(event.target.value)} disabled={selected?.is_system} /></label><label>Code<input value={code} onChange={(event) => setCode(event.target.value)} disabled={Boolean(selected)} placeholder="warehouse_manager" /></label></div><label>Description<input value={description} onChange={(event) => setDescription(event.target.value)} disabled={selected?.is_system} /></label><div className="erp-capability-grid" aria-label="Operation permissions">{Object.entries(operations).map(([resource, operation]) => <div key={resource}><strong>{operation.label}</strong>{actions.map((action) => <label key={action}><input type="checkbox" checked={capabilities.some((item) => item.resource === resource && item.action === action)} onChange={() => toggle(resource, action)} disabled={selected?.is_system} />{action}</label>)}</div>)}</div>{selected && <div className="erp-role-assignment"><strong>Assignments</strong><div><select aria-label="Assign role to user" defaultValue="" onChange={(event) => { const account_id = Number(event.target.value); if (account_id) assignAccount.mutate({ roleId: selected.id, account_id }); event.currentTarget.value = '' }}><option value="">Assign to user…</option>{accounts.data?.filter((account) => !selected.account_assignments.some((assignment) => assignment.account_id === account.id)).map((account) => <option key={account.id} value={account.id}>{account.email}</option>)}</select><select aria-label="Assign role to team" defaultValue="" onChange={(event) => { const team_id = Number(event.target.value); if (team_id) assignTeam.mutate({ roleId: selected.id, team_id }); event.currentTarget.value = '' }}><option value="">Assign to team…</option>{teams.data?.filter((team) => !selected.team_assignments.some((assignment) => assignment.team_id === team.id)).map((team) => <option key={team.id} value={team.id}>{team.name}</option>)}</select></div><small>Assignments are organization-wide by default. Scoped assignments are available through the ERP API for warehouse, project, and branch boundaries.</small></div>}<div className="flex gap-2 mt-4"><button className="primary-action compact" onClick={save} disabled={create.isPending || update.isPending || selected?.is_system}><Save size={14} />Save role</button>{selected && <><button className="secondary-action compact" onClick={() => clone.mutate(selected.id)} disabled={clone.isPending}><Copy size={14} />Clone</button>{!selected.is_system && selected.is_active && <button className="secondary-action compact" onClick={() => deactivate.mutate(selected.id)} disabled={deactivate.isPending}>Deactivate</button>}</>}</div></div></div></section>
}

function FieldRow({ field, fieldTypes, sections, onChange, onRemove }: { field: ERPFormField; fieldTypes: ERPFieldType[]; sections: string[]; onChange: (field: ERPFormField) => void; onRemove: () => void }) {
  return <div className="erp-form-field-row"><input aria-label="Field key" value={field.key} placeholder="field_key" onChange={(event) => onChange({ ...field, key: event.target.value })} /><input aria-label="Field label" value={field.label} placeholder="Label" onChange={(event) => onChange({ ...field, label: event.target.value })} /><select aria-label="Field type" value={field.field_type} onChange={(event) => onChange({ ...field, field_type: event.target.value as ERPFieldType, options: {} })}>{fieldTypes.map((type) => <option key={type}>{type}</option>)}</select><select aria-label="Field section" value={field.section} onChange={(event) => onChange({ ...field, section: event.target.value as ERPFormField['section'] })}>{sections.map((section) => <option key={section}>{section}</option>)}</select><label className="erp-field-required"><input type="checkbox" checked={field.required} onChange={(event) => onChange({ ...field, required: event.target.checked })} />Required</label><button aria-label={`Remove ${field.label || 'field'}`} onClick={onRemove}><X size={15} /></button></div>
}

function FormEditor({ operation, operationInfo }: { operation: string; operationInfo: { sections: Array<'header' | 'line' | 'master'>; label: string } }) {
  const catalog = useERPCatalog(); const definition = useERPForm(operation, true, true); const save = useSaveERPFormDraft(operation); const publish = usePublishERPForm(operation)
  const [fields, setFields] = useState<ERPFormField[]>([]); const [workflow, setWorkflow] = useState<ERPWorkflow>(DEFAULT_WORKFLOW); const [dirty, setDirty] = useState(false)
  useEffect(() => { if (!definition.data || dirty) return; setFields(definition.data.fields); setWorkflow(definition.data.workflow || DEFAULT_WORKFLOW) }, [definition.data, dirty])
  useEffect(() => { setDirty(false) }, [operation])
  const edit = (index: number, next: ERPFormField) => { setFields((all) => all.map((field, current) => current === index ? { ...next, position: current } : field)); setDirty(true) }
  const add = () => { setFields((all) => [...all, { ...emptyField(operationInfo.sections[0]), position: all.length }]); setDirty(true) }
  const saveDraft = async () => { try { await save.mutateAsync({ fields, workflow }); setDirty(false); toast.success('Builder draft saved') } catch (error: any) { toast.error(error.response?.data?.detail?.code || 'Form draft could not be saved') } }
  const publishDraft = async () => { try { await publish.mutateAsync(); setDirty(false); toast.success('Published form version') } catch (error: any) { toast.error(error.response?.data?.detail?.code || 'Form could not be published') } }
  if (definition.isLoading || !catalog.data) return <section className="panel"><p>Loading operation builder…</p></section>
  return <section className="panel erp-builder-panel"><div className="panel-heading"><div><span className="eyebrow">FORM + WORKFLOW</span><h3>{operationInfo.label}</h3><p>Version {definition.data?.version || 1} · {definition.data?.status || 'draft'}{dirty ? ' · unsaved changes' : ''}</p></div><Workflow /></div><div className="erp-form-field-list">{fields.map((field, index) => <FieldRow key={`${field.key}-${index}`} field={field} fieldTypes={catalog.data.field_types} sections={operationInfo.sections} onChange={(next) => edit(index, next)} onRemove={() => { setFields((all) => all.filter((_, current) => current !== index)); setDirty(true) }} />)}</div><button className="secondary-action compact mt-3" onClick={add}><Plus size={14} />Add field</button><details className="erp-workflow-editor"><summary>Workflow states and transitions</summary><label>Workflow JSON<textarea value={JSON.stringify(workflow, null, 2)} onChange={(event) => { try { setWorkflow(JSON.parse(event.target.value)); setDirty(true) } catch { /* preserve editable invalid text until valid JSON */ } }} rows={11} /></label><p>States and transitions are validated when saved; transition role IDs are selected from the access-role list.</p></details><div className="flex gap-2 mt-4"><button className="primary-action compact" onClick={saveDraft} disabled={save.isPending}><Save size={14} />Save draft</button><button className="secondary-action compact" onClick={publishDraft} disabled={publish.isPending || dirty}>Publish version</button></div></section>
}

export function ERPBuilderPanels() {
  const catalog = useERPCatalog(); const roles = useERPAccessRoles(); const [operation, setOperation] = useState('')
  const operations = catalog.data?.operations || {}
  const operationKeys = useMemo(() => Object.keys(operations), [operations])
  useEffect(() => { if (!operation && operationKeys.length) setOperation(operationKeys[0]) }, [operation, operationKeys])
  if (catalog.isLoading || roles.isLoading) return <section className="panel"><p>Loading ERP builders…</p></section>
  if (catalog.isError || roles.isError || !catalog.data) return <section className="panel"><p>ERP builder access is required.</p></section>
  return <div className="flex flex-col gap-4"><RoleEditor roles={roles.data || []} operations={operations} actions={catalog.data.actions} /><section className="panel erp-builder-panel"><div className="view-toolbar"><div><span className="eyebrow">OPERATION BUILDER</span><h3>Versioned forms</h3></div><select aria-label="ERP operation" value={operation} onChange={(event) => setOperation(event.target.value)}>{operationKeys.map((key) => <option value={key} key={key}>{operations[key].label}</option>)}</select></div>{operation && <FormEditor operation={operation} operationInfo={operations[operation]} />}</section></div>
}
