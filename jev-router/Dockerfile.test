FROM node:22-alpine
WORKDIR /app/jev-router
COPY jev-router/package.json jev-router/package-lock.json* ./
RUN npm ci
COPY jev-router/tsconfig.json ./
COPY jev-router/src ./src
CMD ["sh", "-c", "npm test && npm run build"]
